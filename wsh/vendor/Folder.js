export #(p) {
  p.path = ``
}

fix fso = new nativeGlobal.ActiveXObject(`Scripting.FileSystemObject`)

Self.prototype{
  $.getFiles = #(p) {
    
  }
  $.getFolders = #(p) {
    
  }
  
  $.getParentFolder = #(p) {
    
  }
  
  # create folder if not exist
  $.create = #(p) {
    
  }
}